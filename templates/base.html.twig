<!DOCTYPE html>
<html lang="'fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <meta name="robots" content="all">
        <meta name="description" content="Diététique, repas minceur, régime alimentaire">
        <title>{% block title %}Welcome!{% endblock %}</title>


        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            <script src="https://cdn.tailwindcss.com"></script>
            <script src="https://code.jquery.com/jquery-3.6.1.min.js" integrity="sha256-o88AwQnZB+VDvE9tvIXrMQaPlFFSUTR+nldQm1LuPXQ=" crossorigin="anonymous"></script>
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}

        {% endblock %}
    </head>
    <body>
    <nav class="navbar">
        <div class="mx-auto max-w-7xl px-2 sm:px-6 lg:px-8">
            <div class="relative flex h-16 items-center justify-between">
                <div class="absolute inset-y-0 left-0 flex items-center sm:hidden">

                </div>
                <div class="flex flex-1 items-center justify-center sm:items-stretch sm:justify-start">
                    <div class="flex flex-shrink-0 items-center">

                    </div>
                    <div class="hidden sm:ml-6 sm:block">
                        <div class="flex space-x-4">
                            {# templates/home.html.twig #}


                            <!-- Current: "bg-gray-900 text-white", Default: "text-gray-300 hover:bg-gray-700 hover:text-white" -->
                            <a href="{{path('home')}}" class="text-white hover:text-black px-3 py-2 rounded-md text-sm font-medium">A propos de moi</a>

                            <a href="{{path('contact')}}" class="text-white hover:text-black px-3 py-2 rounded-md text-sm font-medium">Contactez-moi!</a>

                            <a href="{{ path('recette') }}" class="text-white hover:text-black px-3 py-2 rounded-md text-sm font-medium">Recettes gratuites</a>
                            {% if app.user %}
                            {% if user.roles is same as ['ROLE_USER'] %}
                                <a href="{{ path('mon_espace') }}" class="text-white hover:text-black px-3 py-2 rounded-md text-sm font-medium">Mon espace</a>
                            {% endif %}
                            {% endif %}


                            {% if app.user %}
                                {% if user.roles is same as ['ROLE_ADMIN'] %}
                            <a href="{{ path('admin') }}" class="text-white hover:text-black px-3 py-2 rounded-md text-sm font-medium">Mon espace Admin</a>
                            {% endif %}
                            {% endif %}


                        </div>
                    </div>
                </div>
                <div class="absolute inset-y-0 right-0 flex items-center pr-2 sm:static sm:inset-auto sm:ml-6 sm:pr-0">

                        {% if app.user %}
                    <a href="{{ path('logout') }}"  type="button" class="rounded-full  p-1 text-red-700 hover:text-white focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-800">
                        <span class="sr-only">View notifications</span>
                        <!-- Heroicon name: outline/power -->
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M5.636 5.636a9 9 0 1012.728 0M12 3v9" />
                        </svg>

                    </a>
                        {% else %}
                    <a href="{{ path('login') }}"  type="button" class="rounded-full  p-1 text-black hover:text-white focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-800">
                        <span class="sr-only">View notifications</span>
                        <!-- Heroicon name: outline/power -->
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M5.636 5.636a9 9 0 1012.728 0M12 3v9" />
                        </svg>

                    </a>
                    {% endif %}
                    <!-- Profile dropdown -->
                    <div class="relative ml-3">




                    </div>
                </div>
            </div>
        </div>

        <!-- Mobile menu, show/hide based on menu state. -->
        <!-- Mobile menu, show/hide based on menu state. -->
        <div class="sm:hidden" id="mobile-menu">
            <div class="space-y-1 px-2 pt-2 pb-3">
                <a href="{{path('home')}}" class="text-white hover:text-black px-3 py-2 rounded-md text-sm font-medium">A propos de moi</a>

                <a href="{{path('contact')}}" class="text-white hover:text-black px-3 py-2 rounded-md text-sm font-medium">Contactez-moi!</a>

                <a href="{{ path('recette') }}" class="text-white hover:text-black px-3 py-2 rounded-md text-sm font-medium">Recettes gratuites</a>
                {% if app.user %}
                    {% if user.roles is same as ['ROLE_USER'] %}
                        <a href="{{ path('mon_espace') }}" class="text-white hover:text-black px-3 py-2 rounded-md text-sm font-medium">Mon espace</a>
                    {% endif %}
                {% endif %}


                {% if app.user %}
                    {% if user.roles is same as ['ROLE_ADMIN'] %}
                        <a href="{{ path('admin') }}" class="text-white hover:text-black px-3 py-2 rounded-md text-sm font-medium">Mon espace Admin</a>
                    {% endif %}
                {% endif %}
            </div>
        </div>
    </nav>
    <br/>
    <H4 class="text-black italic">Sandrine Coupart, diététicienne nutritionniste à votre service!</H4>
    <br/>













    {% block content %}{% endblock %}

    <footer class="">
        <div class="mx-auto max-w-7xl overflow-hidden py-12 px-4 sm:px-6 lg:px-8">
            <nav class="-mx-5 -my-2 flex flex-wrap justify-center" aria-label="Footer">
                <div class="px-5 py-2">
                    <a href="{{path('mention')}}" class="text-base text-gray-500 hover:text-gray-900">Mention légale</a>
                </div>

                <div class="px-5 py-2">
                    <a href="{{path('politique')}}" class="text-base text-gray-500 hover:text-gray-900">Politique de confidentialité</a>
                </div>




            </nav>

            <p class="mt-8 text-center text-base text-gray-400">&copy; 2020 <span><a href="https://thirion-thomas-devweb.vercel.app/">Thomasdevweb94</a> </span>, Inc. All rights reserved.</p>
        </div>
    </footer>





    </body>
</html>
