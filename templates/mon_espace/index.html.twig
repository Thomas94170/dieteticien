{% extends 'base.html.twig' %}

{% block title %}Hello MonEspaceController!{% endblock %}

{% block content %}
    <div class="border-b border-gray-200 bg-white px-4 py-5 sm:px-6">
        <h3 class="text-lg font-medium leading-6 text-gray-900">{{ user.lastname }} {{ user.firstname }}</h3>
        <p class="mt-1 text-sm text-gray-500">type de régime: {{ user.regime }}</p>
    </div>

{% endblock %}
